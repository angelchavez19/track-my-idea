import { confirm } from "./confirm";
import { confirmChange } from "./confirm-change";
import { login } from "./login";
import { refreshEmailVerification } from "./refresh-email-verification";
import { register } from "./register";
import { requestChange } from "./request-change";

export const auth = {
  confirmChange,
  confirm,
  login,
  refreshEmailVerification,
  register,
  requestChange,
};
